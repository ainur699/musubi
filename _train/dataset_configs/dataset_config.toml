[general]
resolution = [1328, 1328]   # = max area of the normalized target images (1.76 MP)
enable_bucket = true
bucket_no_upscale = true    # keep generation images at their native size (no resize)
batch_size = 1

[[datasets]]
image_jsonl_file = "/mnt/images/a.gainetdionov/Datasets/es_hunt_only/es_hunt_qwen_image_reference.jsonl"
cache_directory  = "/mnt/images/a.gainetdionov/Datasets/es_hunt_only/cache_qwen_image_reference"
num_repeats = 1

[[datasets]]
image_jsonl_file = "/mnt/images/a.gainetdionov/Datasets/sft_dataset/babesource/babesource_qwen_image_reference.jsonl"
cache_directory  = "/mnt/images/a.gainetdionov/Datasets/sft_dataset/babesource/cache_qwen_image_reference"
num_repeats = 1

# Control (condition / ref_person) images ARE resized to the official Edit size.
control_resolution = [1024, 1024]
no_resize_control = false
